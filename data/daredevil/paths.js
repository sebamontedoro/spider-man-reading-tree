/**
 * Curated reading paths through the Daredevil tree. Empty until curated; shape
 * as in data/spider-man/paths.js.
 */

export const PATHS = []

export const PATHS_BY_KEY = Object.fromEntries(PATHS.map((p) => [p.key, p]))
