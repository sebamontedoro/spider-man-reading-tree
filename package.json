{
  "name": "spider-man-reading-tree",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:data && vite build",
    "preview": "vite preview",
    "build:data": "node scripts/build-dataset.mjs",
    "verify:wiki": "node scripts/verify-wiki.mjs",
    "dev:reader": "COMICS_DIR=${COMICS_DIR:-./comics} CACHE_DIR=${CACHE_DIR:-./.cache/pages} node reader/server.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
